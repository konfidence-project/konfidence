---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: ocmcomponentversionocis.common.konfidence.tools.sap
spec:
  group: common.konfidence.tools.sap
  names:
    kind: OCMComponentVersionOCI
    listKind: OCMComponentVersionOCIList
    plural: ocmcomponentversionocis
    singular: ocmcomponentversionoci
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OCMComponentVersionOCI is the Schema for the ocmcomponentversionocis
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OCMComponentVersionOCISpec defines the desired state of OCMComponentVersionOCI.
            properties:
              component:
                description: Component specifies the name of the ComponentVersion.
                type: string
              repository:
                description: |-
                  Repository provides details about the OCI repository from which the component
                  descriptor can be retrieved.
                properties:
                  secretRef:
                    description: SecretRef specifies the credentials used to access
                      the OCI registry.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  url:
                    description: |-
                      URL specifies the URL of the OCI registry in which the ComponentVersion is stored.
                      MUST NOT CONTAIN THE SCHEME.
                    type: string
                required:
                - url
                type: object
              version:
                description: Version specifies the version information for the ComponentVersion.
                type: string
            required:
            - component
            - repository
            - version
            type: object
          status:
            description: OCMComponentVersionOCIStatus defines the observed state of
              OCMComponentVersionOCI.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
