apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: stageconfigurations.global.konfidence.cloud
spec:
  group: galaxy.konfidence.cloud
  names:
    kind: StageConfiguration
    listKind: StageConfigurationList
    plural: stageconfigurations
    singular: stageconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.vector
      name: Vector
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StageConfiguration is the Schema for the stageConfigurations
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StageConfigurationSpec defines the desired state of StageConfiguration.
            properties:
              config:
                items:
                  description: CredentialsConfig defines a credential reference to
                    a secret or configMap used to access OCM backends (like OCI registry).
                  properties:
                    apiVersion:
                      description: APIVersion is the api version of the of configuration
                        resource, e.g. v1.
                      type: string
                    kind:
                      description: Kind of the configuration resource. Allowed values
                        are Secret or ConfigMap.
                      type: string
                    name:
                      description: "Name is the name\t of the of configuration resource."
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              name:
                description: Name is the stage name
                type: string
              targetNamespace:
                description: TargetNamespace is the target namespace where the associated
                  stage is created or updated
                type: string
              targetWorkspace:
                description: TargetWorkspace is the target workspace where the associated
                  stage is created or updated
                type: string
              vector:
                description: Vector points to the OCM component that contains the
                  deployment vector for this stage.
                type: string
            required:
            - name
            - targetNamespace
            - vector
            type: object
          status:
            description: StageConfigurationStatus defines the observed state of StageConfiguration.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: stagesyncs.global.konfidence.cloud
spec:
  group: galaxy.konfidence.cloud
  names:
    kind: StageSync
    listKind: StageSyncList
    plural: stagesyncs
    singular: stagesync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Applied status
      jsonPath: .status.conditions[?(@.type=='Applied')].status
      name: Applied
      type: string
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StageSync is the Schema for the stageSyncs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StageSyncSpec defines the desired state of a StageSync.
            properties:
              reconcileInterval:
                description: |-
                  ReconcileInterval defines how often the sync controller should reconcile the StageSync resource.
                  If not set, the controller's default reconcile interval will be used.
                type: string
              stageTemplate:
                description: StageTemplate contains the template of the stage to be
                  created on the Star cluster.
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - stageTemplate
            type: object
          status:
            description: StageSyncStatus defines the observed state of a StageSync.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              stageStatus:
                type: object
                x-kubernetes-preserve-unknown-fields: true
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: vectorpromotionconfigs.global.konfidence.cloud
spec:
  group: galaxy.konfidence.cloud
  names:
    kind: VectorPromotionConfig
    listKind: VectorPromotionConfigList
    plural: vectorpromotionconfigs
    singular: vectorpromotionconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Last promotion succeeded
      jsonPath: .status.lastPromotionConditions[0].status
      name: Last-Succeeded
      type: string
    - description: Last promotion condition reason
      jsonPath: .status.lastPromotionConditions[0].reason
      name: Last-Condition-Reason
      type: string
    - description: Time of the last promotion
      jsonPath: .status.lastPromotionConditions[0].lastTransitionTime
      name: Last-Time
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorPromotionConfig describes a promotion flow for a vector
          between a source and a target.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          config:
            items:
              description: CredentialsConfig defines a credential reference to a secret
                or configMap used to access OCM backends (like OCI registry).
              properties:
                apiVersion:
                  description: APIVersion is the api version of the of configuration
                    resource, e.g. v1.
                  type: string
                kind:
                  description: Kind of the configuration resource. Allowed values
                    are Secret or ConfigMap.
                  type: string
                name:
                  description: "Name is the name\t of the of configuration resource."
                  type: string
              required:
              - apiVersion
              - kind
              - name
              type: object
            type: array
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the VectorPromotionConfig.
            properties:
              source:
                description: |-
                  Source is the OCM component reference to promote from.
                  This usually points to a version alias (e.g. :latest) that resolves to the component version to be promoted.
                  The format is <registry>//<component-name>:<version>.
                minLength: 1
                pattern: ^[^/].+//.+:.+$
                type: string
                x-kubernetes-validations:
                - message: source is immutable after it has been set
                  rule: self == oldSelf
              target:
                description: |-
                  Target is the OCM component reference to promote to.
                  This usually points to a version alias (e.g. :promoted). The actual version string is taken from the source component version.
                  The format is <registry>//<component-name>:<version>.
                minLength: 1
                pattern: ^[^/].+//.+:.+$
                type: string
                x-kubernetes-validations:
                - message: target is immutable after it has been set
                  rule: self == oldSelf
            required:
            - source
            - target
            type: object
          status:
            description: VectorPromotionConfigStatus defines the observed state of
              VectorPromotionConfig.
            properties:
              lastPromotionConditions:
                description: LastPromotionConditions contains the result of the most
                  recent VectorPromotion execution
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSuccessfulPromotionConditions:
                description: LastSuccessfulPromotionConditions contains the result
                  of the most recent VectorPromotion execution, that was successful
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: Spec is required once set
          rule: '!has(oldSelf.spec) || has(self.spec)'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: vectorpromotions.global.konfidence.cloud
spec:
  group: galaxy.konfidence.cloud
  names:
    kind: VectorPromotion
    listKind: VectorPromotionList
    plural: vectorpromotions
    singular: vectorpromotion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The referenced VectorPromotionConfig
      jsonPath: .spec.vectorPromotionConfigRef
      name: Config
      type: string
    - description: Promotion was successful
      jsonPath: .status.conditions[0].status
      name: Promotion Succeeded
      type: string
    - description: Promotion condition reason
      jsonPath: .status.conditions[0].reason
      name: Reason
      type: string
    - description: Age
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorPromotion triggers a one-time execution of a promotion
          flow defined by a VectorPromotionConfig.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VectorPromotionSpec defines the desired state of VectorPromotion.
            properties:
              ttlAfterFinished:
                description: |-
                  TTLAfterFinished defines how long the VectorPromotion should be kept after completion.
                  Once the TTL expires after the promotion reaches a terminal state (Completed or Failed),
                  the resource is eligible for automatic deletion. If no TTL is set, no deletion happens.
                type: string
              vectorPromotionConfigRef:
                description: VectorPromotionConfigRef is the name of the VectorPromotionConfig
                  that defines the promotion flow to execute.
                minLength: 1
                type: string
            required:
            - vectorPromotionConfigRef
            type: object
          status:
            description: VectorPromotionStatus defines the observed state of VectorPromotion.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: vectortemplates.global.konfidence.cloud
spec:
  group: galaxy.konfidence.cloud
  names:
    categories:
    - konfidence
    - kden
    kind: VectorTemplate
    listKind: VectorTemplateList
    plural: vectortemplates
    singular: vectortemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Indicates if the vector template is ready
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - description: The reason of the Ready condition
      jsonPath: .status.conditions[?(@.type=="Ready")].reason
      name: Reason
      type: string
    - description: The upload target of the vector template
      jsonPath: .spec.uploadTarget
      name: Upload-Target
      type: string
    - description: Time since creation of the vector template
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VectorTemplate represents a template for assembling OCM components into an OCM component
          that represents a vector.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VectorTemplate
            properties:
              base:
                description: Base represents an optional base component version to
                  build upon.
                type: string
              components:
                description: Components lists the components to be included in the
                  vector.
                items:
                  description: |-
                    Component defines a component of a VectorTemplate.
                    A struct is used for future expansion.
                  properties:
                    name:
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              config:
                items:
                  description: CredentialsConfig defines a credential reference to
                    a secret or configMap used to access OCM backends (like OCI registry).
                  properties:
                    apiVersion:
                      description: APIVersion is the api version of the of configuration
                        resource, e.g. v1.
                      type: string
                    kind:
                      description: Kind of the configuration resource. Allowed values
                        are Secret or ConfigMap.
                      type: string
                    name:
                      description: "Name is the name\t of the of configuration resource."
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  type: object
                type: array
              reconcileInterval:
                description: |-
                  ReconcileInterval defines how often the assembly controller should check for drift.
                  If not set, the controller's default reconcile interval will be used.
                type: string
              uploadTarget:
                description: UploadTarget defines the target OCM component where the
                  assembled vector will be uploaded.
                type: string
            required:
            - components
            - uploadTarget
            type: object
          status:
            description: status defines the observed state of VectorTemplate
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
