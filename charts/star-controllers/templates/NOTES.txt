Konfidence Star controllers {{ .Chart.AppVersion }} installed in namespace {{ .Release.Namespace }}.

Verify the controller is running:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Tail logs:
  kubectl -n {{ .Release.Namespace }} logs -l app.kubernetes.io/instance={{ .Release.Name }} -f

{{- if index .Values "star-crds" "enabled" }}
CRDs were installed by the bundled star-crds subchart.
{{- else }}
CRD installation was disabled (star-crds.enabled=false). Make sure the
star CRDs are present in the cluster, otherwise reconciliation will fail:
  kubectl get crd | grep star.konfidence.cloud
{{- end }}
