Konfidence Star CRDs {{ .Chart.AppVersion }} installed.

Installed CustomResourceDefinitions (group: star.konfidence.cloud):
  - activationtaskexecutions
  - activationtaskregistrations
  - artifactdeployments
  - stages
  - stageversions
  - stageversionusages
  - taskexecutions
  - vectoractivations
  - vectorassignments
  - vectordeployments
  - vectormigrations

Note: With keepOnUninstall=true (default), these CRDs are retained on
`helm uninstall` to protect existing custom resources. Remove them
manually with `kubectl delete crd <name>` if you truly want them gone.
