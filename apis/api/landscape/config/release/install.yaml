apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: activationtaskexecutions.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: ActivationTaskExecution
    listKind: ActivationTaskExecutionList
    plural: activationtaskexecutions
    singular: activationtaskexecution
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of the task execution
      jsonPath: .spec.type
      name: Type
      type: string
    - description: The associated VectorActivation
      jsonPath: .spec.vectorActivation
      name: Vector-Activation
      type: string
    - description: The latest condition type
      jsonPath: .status.conditions[-1:].type
      name: Condition
      type: string
    - description: The latest condition status
      jsonPath: .status.conditions[-1:].status
      name: Condition-Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ActivationTaskExecution is the Schema for the ActivationTaskExecutions
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ActivationTaskExecution
            properties:
              spec:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              type:
                type: string
              vectorActivation:
                description: VectorActivation is a temporary field that contains the
                  name of the associated vectorActivation
                type: string
            required:
            - spec
            - type
            - vectorActivation
            type: object
          status:
            description: status defines the observed state of ActivationTaskExecution
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: activationtaskregistrations.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: ActivationTaskRegistration
    listKind: ActivationTaskRegistrationList
    plural: activationtaskregistrations
    singular: activationtaskregistration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of registered activation task
      jsonPath: .spec.type
      name: Type
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ActivationTaskRegistration is the Schema for the activationtaskregistrations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ActivationTaskRegistration
            properties:
              precedes:
                items:
                  type: string
                type: array
              spec:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              succeeds:
                items:
                  type: string
                type: array
              type:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  The following markers will use OpenAPI v3 schema to validate the value
                  More info: https://book.kubebuilder.io/reference/markers/crd-validation.html
                type: string
            required:
            - spec
            - type
            type: object
          status:
            description: status defines the observed state of ActivationTaskRegistration
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ActivationTaskRegistration resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: artifactdeployments.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: ArtifactDeployment
    listKind: ArtifactDeploymentList
    plural: artifactdeployments
    singular: artifactdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="ArtifactFetched")].status
      name: Fetched
      type: string
    - jsonPath: .status.conditions[?(@.type=="ArtifactDeployed")].status
      name: Deployed
      type: string
    - jsonPath: .status.conditions[?(@.type=="AppHealthy")].message
      name: Healthy
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ArtifactDeployment is the Schema for the artifactdeployments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the ArtifactDeployment
              and is immutable after it has been set
            properties:
              component:
                description: Component contains OCM metadata associated with the artifact.
                  This is a simplified mapping of the OCM ComponentVersion.
                properties:
                  name:
                    description: Name is the OCM ComponentVersion name.
                    type: string
                  resources:
                    description: |-
                      Resources contains OCM resources belonging to this component. The structure is intentionally generic to support
                      the requirements of deployers targeting different runtimes.
                    items:
                      description: |-
                        OCMResource represents a single resource of an OCM ComponentVersion. The content and type are deployer-specific and
                        opaque to the API.
                      properties:
                        content:
                          description: |-
                            Content holds raw resource data, typically an embedded manifest, file, or
                            binary payload.
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        name:
                          description: Name is the resource name.
                          type: string
                        type:
                          description: Type describes the resource type, following
                            OCM conventions.
                          type: string
                      required:
                      - content
                      - name
                      - type
                      type: object
                    type: array
                  version:
                    description: Version is the OCM ComponentVersion version.
                    type: string
                required:
                - name
                type: object
              manifest:
                description: Manifest contains information about the artifact itself
                  and the deployer implementation responsible for handling it.
                properties:
                  allowReuse:
                    description: |-
                      AllowReuse indicates whether the deployed artifact instance may be shared across multiple VectorDeployments.
                      Reuse allows more efficient resource consumption but requires the artifact to be independent of vector-specific
                      runtime context.
                    type: boolean
                  type:
                    description: |-
                      Type specifies the deployer that should handle this artifact (e.g., "cloud.konfidence.flux.helm",
                      "cloud.konfidence.flux.kustomize", or any custom deployer type). Deployers implement their own interpretation
                      of the artifact's contents.
                    type: string
                required:
                - allowReuse
                - type
                type: object
              taskManifests:
                description: |-
                  TaskManifests describes optional post-deployment tasks (commonly used for vector migrations such as database
                  schema updates). Tasks are executed after the artifact has been deployed and may form a dependency graph via
                  DependsOn.
                items:
                  description: |-
                    TaskManifest defines a post-deployment task that is executed after the artifact has been deployed. Tasks are
                    commonly used for vector migrations (such as database schema changes) but may represent any post-deployment action.

                    Tasks form a directed acyclic graph (DAG) at the *vector level* rather than only within a single ArtifactDeployment.
                    A task may depend on tasks belonging to other microservices or artifacts in the same VectorDeployment. These
                    cross-artifact dependencies allow defining a globally ordered migration or transformation workflow.

                    The controller responsible for the task type interprets the Spec field and performs the execution once all declared
                    dependencies have completed successfully.
                  properties:
                    dependsOn:
                      description: |-
                        DependsOn lists names of other tasks that must complete before this task may run. Dependencies may reference
                        tasks within the same artifact or any other artifact that participates in the same VectorDeployment, allowing the
                        formation of a vector-wide DAG.
                      items:
                        type: string
                      type: array
                    name:
                      description: |-
                        Name uniquely identifies this task within the entire vector. This name may be referenced by other tasks across
                        different artifacts.
                      type: string
                    spec:
                      description: |-
                        Spec contains task-specific configuration. The structure depends on the task Type and is interpreted by the
                        corresponding task controller.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: |-
                        Type specifies the task controller or execution runtime (e.g. "k8s-job", or any custom task runtime). Different
                        task types correspond to different task controllers, each interpreting the Spec field according to their own semantics.
                      type: string
                  required:
                  - name
                  - spec
                  - type
                  type: object
                type: array
            required:
            - manifest
            - taskManifests
            type: object
            x-kubernetes-validations:
            - message: ArtifactDeployment spec is immutable after it has been set
              rule: self == oldSelf
          status:
            description: ArtifactDeploymentStatus defines the observed state of ArtifactDeployment.
            properties:
              conditions:
                description: |-
                  Conditions describes the state of the deployment lifecycle. The following conditions are expected:

                    - ArtifactFetched: the artifact was successfully retrieved
                    - ArtifactDeployed: the artifact was successfully deployed
                    - AppHealthy: the deployer reports the workload as healthy

                  Conditions progress in a linear order:
                  ArtifactFetched -> ArtifactDeployed -> AppHealthy
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deploymentResult:
                description: |-
                  DeploymentResults captures structured outputs produced by the deployer during the deployment process—such as
                  computed DNS names, service endpoints, generated configuration, or other workload-specific details.

                  Results should be treated as immutable for a given generation and may be consumed by later stages of a vector
                  rollout (e.g., routing configuration).

                  Each result must have a unique Name.
                items:
                  description: |-
                    DeploymentResult contains a single output produced by a deployer. These results are used to transport information
                    from the deployer to later phases of the vector lifecycle.
                  properties:
                    name:
                      description: Name is a unique identifier for the result within
                        an ArtifactDeploymentStatus.
                      type: string
                    spec:
                      description: Spec contains deployer-specific structured data.
                        Its format is determined by the Type field.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: Type describes the structure contained in Spec.
                        Each deployer may define multiple result types.
                      type: string
                  required:
                  - name
                  - spec
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: Spec is required once set
          rule: '!has(oldSelf.spec) || has(self.spec)'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: stageversions.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: StageVersion
    listKind: StageVersionList
    plural: stageversions
    singular: stageversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The object generation of the stage
      jsonPath: .spec.stage_generation
      name: Stage-Generation
      type: integer
    - description: The deployment vector for this stage
      jsonPath: .spec.vector
      name: Vector
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StageVersion is the Schema for the stageversions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the StageVersion and is
              immutable after it has been set
            properties:
              stageGeneration:
                description: the object generation of the stage that created this
                  stage version
                format: int64
                minimum: 1
                type: integer
              stageRef:
                description: StageReference holds a reference to a Stage in the same
                  namespace.
                properties:
                  name:
                    description: Name is the name of the Stage. Required.
                    type: string
                required:
                - name
                type: object
              vector:
                description: Vector points to the OCM component version that contains
                  the deployment vector for this stage.
                minLength: 1
                type: string
            required:
            - stageGeneration
            - stageRef
            - vector
            type: object
            x-kubernetes-validations:
            - message: StageVersion spec is immutable after it has been set
              rule: self == oldSelf
          status:
            description: status defines the observed state of StageVersion
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: Spec is required once set
          rule: '!has(oldSelf.spec) || has(self.spec)'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: stageversionusages.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: StageVersionUsage
    listKind: StageVersionUsageList
    plural: stageversionusages
    singular: stageversionusage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The referenced StageVersion
      jsonPath: .spec.stageVersionRef.name
      name: Stage-Version-Ref
      type: string
    - description: The label selector for the StageVersion
      jsonPath: .spec.stageVersionSelector
      name: Stage-Version-Selector
      type: string
    - description: The reason for this StageVersion usage
      jsonPath: .spec.reason
      name: Reason
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StageVersionUsage is the Schema for the stageversionusages API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StageVersionUsage
            properties:
              reason:
                description: Reason is human-readable description of why this StageVersion
                  is in use, e.g. "executing vector migrations", "latest vector for
                  stage xyz",
                type: string
              stageVersionRef:
                description: StageVersionRef references a stageVersion
                properties:
                  name:
                    description: Name is the name of the StageVersion. Required.
                    type: string
                required:
                - name
                type: object
              stageVersionSelector:
                description: StageVersionSelector is a label selector to find a StageVersion
                  when name is not provided.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            type: object
            x-kubernetes-validations:
            - message: exactly one of the fields in [stageVersionRef stageVersionSelector]
                must be set
              rule: '[has(self.stageVersionRef),has(self.stageVersionSelector)].filter(x,x==true).size()
                == 1'
          status:
            description: status defines the observed state of StageVersionUsage
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              resolvedStageVersions:
                description: ResolvedStageVersions contains the names of all resolved
                  stageVersion resources specified by either stageVersionRef or StageVersionSelector
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: taskexecutions.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: TaskExecution
    listKind: TaskExecutionList
    plural: taskexecutions
    singular: taskexecution
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of the task execution
      jsonPath: .spec.type
      name: Type
      type: string
    - description: The latest condition type
      jsonPath: .status.conditions[-1:].type
      name: Condition
      type: string
    - description: The latest condition status
      jsonPath: .status.conditions[-1:].status
      name: Condition-Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TaskExecution is the Schema for the taskexecutions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TaskExecution
            properties:
              dependsOn:
                items:
                  type: string
                type: array
              name:
                type: string
              spec:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              type:
                type: string
            required:
            - name
            - spec
            - type
            type: object
          status:
            description: status defines the observed state of TaskExecution
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vectoractivations.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: VectorActivation
    listKind: VectorActivationList
    plural: vectoractivations
    singular: vectoractivation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The stage of the activation
      jsonPath: .spec.stage
      name: Stage
      type: string
    - description: The version of the stage
      jsonPath: .spec.stageVersion
      name: Stage-Version
      type: string
    - description: The latest condition type
      jsonPath: .status.conditions[-1:].type
      name: Condition
      type: string
    - description: The latest condition status
      jsonPath: .status.conditions[-1:].status
      name: Condition-Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorActivation is the Schema for the vectoractivations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VectorActivation
            properties:
              stage:
                type: string
              stageVersion:
                type: string
              vector:
                description: Vector points to the OCM component version that contains
                  the deployment vector for this stage.
                type: string
              vectorDeployment:
                type: string
            required:
            - stage
            - stageVersion
            - vector
            - vectorDeployment
            type: object
          status:
            description: status defines the observed state of VectorActivation
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vectorassignments.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: VectorAssignment
    listKind: VectorAssignmentList
    plural: vectorassignments
    singular: vectorassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The name of the ArtifactDeployment
      jsonPath: .spec.artifactDeploymentRef.name
      name: Artifact-Deployment
      type: string
    - description: The name of the VectorDeployment
      jsonPath: .spec.vectorDeploymentRef.name
      name: Vector-Deployment
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VectorAssignment is the Schema for the vectorassignments API.

          A VectorAssignment represents a single binding between a VectorDeployment and an ArtifactDeployment. It enables
          an n:m mapping where a single artifact may be reused across multiple vectors. These objects are automatically
          managed by the vector-deployment-controller and reconciled by deployers to apply vector-specific configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the VectorAssignment and
              is immutable after it has been set
            properties:
              artifactDeploymentRef:
                description: |-
                  ArtifactDeploymentRef references the ArtifactDeployment instance that is associated with the vector. The
                  referenced artifact must exist in the same namespace as this VectorAssignment.
                properties:
                  name:
                    description: "Name\tis the name of the ArtifactDeployment. Required."
                    type: string
                required:
                - name
                type: object
              manifest:
                description: |-
                  Manifest contains the ArtifactManifest describing the artifact to be assigned to the vector. This duplicates the
                  manifest stored in the ArtifactDeployment for efficiency: deployers often need to filter or select assignments
                  by artifact type, and embedding the manifest avoids repeated API lookups.
                properties:
                  allowReuse:
                    description: |-
                      AllowReuse indicates whether the deployed artifact instance may be shared across multiple VectorDeployments.
                      Reuse allows more efficient resource consumption but requires the artifact to be independent of vector-specific
                      runtime context.
                    type: boolean
                  type:
                    description: |-
                      Type specifies the deployer that should handle this artifact (e.g., "cloud.konfidence.flux.helm",
                      "cloud.konfidence.flux.kustomize", or any custom deployer type). Deployers implement their own interpretation
                      of the artifact's contents.
                    type: string
                required:
                - allowReuse
                - type
                type: object
              vectorDeploymentRef:
                description: |-
                  VectorDeploymentRef references the VectorDeployment that this artifact is assigned to. This creates the explicit
                  mapping "artifact X belongs to vector Y".
                properties:
                  name:
                    description: "Name\tis the name of the VectorDeployment. Required."
                    type: string
                required:
                - name
                type: object
            required:
            - artifactDeploymentRef
            - manifest
            - vectorDeploymentRef
            type: object
            x-kubernetes-validations:
            - message: VectorAssignment spec is immutable after it has been set
              rule: self == oldSelf
          status:
            description: |-
              VectorAssignmentStatus defines the observed state of a VectorAssignment.

              A VectorAssignment progresses through a simple lifecycle driven by the deployer:

               1. VectorAssignment is created by the vector-deployment-controller.
               2. deployer reconciles it and configures vector-specific integration
               3. VectorAssignedCondition is set to True
            properties:
              conditions:
                description: |-
                  Conditions describes the latest observed state of the assignment. The primary condition is
                  VectorAssignedCondition, which becomes True once the deployer has finished processing the VectorAssignment.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
        x-kubernetes-validations:
        - message: Spec is required once set
          rule: '!has(oldSelf.spec) || has(self.spec)'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vectordeployments.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: VectorDeployment
    listKind: VectorDeploymentList
    plural: vectordeployments
    singular: vectordeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The deployment vector
      jsonPath: .spec.vector
      name: Vector
      type: string
    - description: Indicates if the vector is ready
      jsonPath: .status.conditions[?(@.type=="VectorReady")].status
      name: Vector-Ready
      type: string
    - description: The reason of the VectorReady condition
      jsonPath: .status.conditions[?(@.type=="VectorReady")].reason
      name: Reason
      type: string
    - description: The message of the VectorReady condition
      jsonPath: .status.conditions[?(@.type=="VectorReady")].message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VectorDeployment is the Schema for the vectordeployments API.

          VectorDeployment represents the deployment of an immutable vector of artifacts into a specific environment or stage.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the VectorDeployment and
              is immutable after it has been set
            properties:
              vector:
                description: |-
                  Vector is a fully qualified URL pointing to an OCM ComponentVersion stored in an OCI registry. The referenced
                  component contains the deployment vector, which includes the complete list of artifacts and their versions.
                type: string
            required:
            - vector
            type: object
            x-kubernetes-validations:
            - message: VectorDeployment spec is immutable after it has been set
              rule: self == oldSelf
          status:
            description: |-
              VectorDeploymentStatus represents the observed state of a VectorDeployment as it progresses through the
              deployment lifecycle.

              The lifecycle consists of:
               1. Pulling the vector from the OCI registry and parsing its contents -> VectorDownloadedCondition
               2. Creating (or re-using) one ArtifactDeployment per artifact in the vector -> ArtifactDeploymentsCreatedCondition
               3. Waiting until all ArtifactDeployments have successfully deployed -> VectorDeployedCondition
               4. Creating all VectorAssignment resources associated with this vector -> VectorAssignmentsCreatedCondition
               5. Marking the vector as ready for use -> VectorReadyCondition
            properties:
              conditions:
                description: |-
                  Conditions represents the current set of status conditions for this vector
                  deployment. These conditions track progress through the lifecycle stages.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deploymentResults:
                additionalProperties:
                  description: |-
                    DeploymentResult contains a single output produced by a deployer. These results are used to transport information
                    from the deployer to later phases of the vector lifecycle.
                  properties:
                    name:
                      description: Name is a unique identifier for the result within
                        an ArtifactDeploymentStatus.
                      type: string
                    spec:
                      description: Spec contains deployer-specific structured data.
                        Its format is determined by the Type field.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: Type describes the structure contained in Spec.
                        Each deployer may define multiple result types.
                      type: string
                  required:
                  - name
                  - spec
                  - type
                  type: object
                description: |-
                  DeploymentResults exposes an aggregated view of the deployment results produced
                  by all underlying ArtifactDeployments. The map key is composed of the component
                  name and the individual result name, ensuring uniqueness.
                type: object
              resolvedVectorOcm:
                description: |-
                  ResolvedVectorOcm contains the fully materialized content of the OCM ComponentVersion after it has been
                  downloaded and resolved from the OCI registry. Unlike the Spec.Vector value, which is only a reference (URL),
                  this field stores the actual resolved vector content as provided by OCM, including all artifacts and metadata.
                  It is not a reference but the inlined representation of the component version at reconciliation time.
                type: string
              resultingArtifactDeployments:
                additionalProperties:
                  description: LocalArtifactDeploymentReference holds a reference
                    to an ArtifactDeployment in the same namespace.
                  properties:
                    name:
                      description: "Name\tis the name of the ArtifactDeployment. Required."
                      type: string
                  required:
                  - name
                  type: object
                description: |-
                  ResultingArtifactDeployments lists the ArtifactDeployment resources created (or re-used) for this vector. The
                  map key is the component name of the artifact as defined inside the vector. Keys remain stable across
                  reconciliations and re-creations.
                type: object
              resultingVectorAssignments:
                additionalProperties:
                  description: LocalVectorAssignmentReference holds a reference to
                    a VectorAssignment in the same namespace.
                  properties:
                    name:
                      description: "Name\tis the name of the VectorAssignment. Required."
                      type: string
                  required:
                  - name
                  type: object
                description: |-
                  ResultingVectorAssignments lists all VectorAssignment resources created for this vector. VectorAssignments are
                  not re-used like ArtifactDeployments, but instead each VectorDeployment results in a complete new set of
                  assignments.

                  The map key is the component name of the artifact. Keys are stable across reconcilations.
                type: object
            type: object
        type: object
        x-kubernetes-validations:
        - message: Spec is required once set
          rule: '!has(oldSelf.spec) || has(self.spec)'
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vectormigrations.landscape.konfidence.cloud
spec:
  group: landscape.konfidence.cloud
  names:
    kind: VectorMigration
    listKind: VectorMigrationList
    plural: vectormigrations
    singular: vectormigration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The stage version
      jsonPath: .spec.stageVersion
      name: Stage-Version
      type: string
    - description: The deployment vector
      jsonPath: .spec.vector
      name: Vector
      type: string
    - description: The latest condition type
      jsonPath: .status.conditions[-1:].type
      name: Condition
      type: string
    - description: The latest condition status
      jsonPath: .status.conditions[-1:].status
      name: Condition-Status
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorMigration is the Schema for the vectormigrations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VectorMigration
            properties:
              stageVersion:
                type: string
              vector:
                description: Vector points to the OCM component version that contains
                  the deployment vector for this stage.
                type: string
            required:
            - stageVersion
            - vector
            type: object
          status:
            description: status defines the observed state of VectorMigration
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
